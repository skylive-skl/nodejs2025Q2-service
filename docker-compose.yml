version: '3.2'

services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - ${PORT}:${PORT}
    networks:
      - webnet



networks:
  webnet: